/*var expect = require('expect.js');
import GroupsModel from '../js/Groups/GroupsModel.js';


describe('Groups Model', function () {
  it('should add a new group', function () {
    var groupModel = new GroupsModel([{
      id: 1,
      name: 'pepe1',
      users: [{ name: "pipo", id: 1}]
    }]);
    var newGroup = {
      name: 'pepe2',
      users: []
    };
    var groupSaved = groupModel.addNewGroup(newGroup);

    expect(groupSaved.name).to.eql(newGroup.name);
  });
});
*/
